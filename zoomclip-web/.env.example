# No server-side secrets needed — everything runs in browser
# Add these if you add Stripe later:
# STRIPE_SECRET_KEY=
# STRIPE_WEBHOOK_SECRET=
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=

NEXT_PUBLIC_APP_URL=http://localhost:3000
